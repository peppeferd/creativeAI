import React from 'react'
export const metadata = {
  title: 'Creative AI',
  description: 'Creative AI - About',
}

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>
}
export default layout
